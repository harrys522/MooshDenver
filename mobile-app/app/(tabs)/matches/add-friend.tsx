// Qr code should be name, icp id (just a string), and maybe a pic if you have time
import { View } from 'react-native';

export default function addFriendScreen() {
    return (
        <View>

        </View>
    )
}